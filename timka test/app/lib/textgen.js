// Пример генерации подписи — позже подключите свою логику
export async function genCaption(selected){
  return [
    'По делу. Без позы.',
    'Суть важнее формы. Сегодня — работать.',
    '#discipline #focus #sigma'
  ].join('\n');
}
